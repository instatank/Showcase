# DayOS screenshots

`data/apps.ts` references these four files (each marked `real: true`, so they
render as actual images, not placeholders). Drop the PNGs here with these exact
names:

| File                  | Slot       | Caption |
| :-------------------- | :--------- | :------ |
| `08todaylight.png`    | Hero       | The day, laid out as time blocks — focus task, logs, and captures in one view. |
| `04trendsovertime.png`| Supporting | Time intelligence: where the hours actually went, across categories and projects. |
| `07captureorganize.png`| Supporting | Journaling with one-tap AI cleanup — messy thoughts in, structured entry out. |
| `projects.png`        | Supporting | Project sessions and learning logs — tagged, dated, searchable. |

Do NOT add `journal.PNG` — it contains real personal journal content. Swap for a
seeded-demo version before this page is public.
