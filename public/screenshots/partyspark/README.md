# PartySpark screenshots

`data/apps.ts` references these files. Drop the PNGs here with these exact
names, then set `real: true` on each matching visual in `data/apps.ts` so they
render as real images instead of labelled placeholders.

| File                   | Slot       | Source screen (from party-spark) | Caption |
| :--------------------- | :--------- | :------------------------------- | :------ |
| `home-grid.png`        | Hero       | Home / game grid                 | 15+ games in one app, filtered by vibe — in a premium glass design system. |
| `roast-me.png`         | Supporting | Roast Me result                  | Upload a photo, get an AI-generated roast and caricature — the signature party moment. |
| `create-your-vibe.png` | Supporting | Create Your Vibe / sub-category  | Type a vibe and AI spins up a custom question pack live — with bundled decks as fallback. |
| `in-play-card.png`     | Supporting | In-play gamecard (e.g. Taboo)    | Tinder-style swipe play and live leaderboards, in one consistent design system. |
| `scramble.png`         | Supporting | Scramble solo word game          | Scramble: a clean solo word game with a live timer and end-of-round scoring. |

Source: `instatank/party-spark`, branch `vibrant-euler`, under `showcase-dark`
and `showcase-light`. Mix light and dark across the set.

**Do NOT include:** the PIN-entry screen or any explicit adult-content deck.
Keep it mature-but-tasteful. Use a public-safe demo photo for Roast Me.
